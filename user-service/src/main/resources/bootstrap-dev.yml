spring:
  cloud:
    config:
      enabled: false
  jpa:
    show-sql: true
eureka:
  client:
    enabled: false

server:
  port: 8801

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
