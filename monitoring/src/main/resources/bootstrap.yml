spring:
  profiles:
    active: default
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config:8888
      label: dev
      fail-fast: true
      retry:
        max-attempts: 60